#include <stdio.h>
#include "cguieng.c"

int main() {
	CREATE_DISPLAY_BUFFER

	if(displayBuffer == NULL)
		printf("\nNO DISPLAY!!\n");
	else
		printf("\nDISPLAY AT: %p\n", displayBuffer);

	printf("before object\n");
	Object* object = NewObject(49, 19);
	Object* object2 = NewObject(49, 19);
	Object* object3 = NewObject(49, 19);
	Object* object4 = NewObject(49, 19);

	
	printf("\nObject from index %d has id %d\n", 0, object->id);
	printf("\nObject from index %d has id %d\n", 1, object2->id);
	printf("\nObject from index %d has id %d\n", 2, object3->id);
	printf("\nObject from index %d has id %d\n", 3, object4->id);
	
	DestroyObject(&object3);
	if(object == NULL)
		printf("OBJECT NULLIFIED!!\n");
	return 0;
}